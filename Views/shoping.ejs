<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Shopping - Liste des Produits</title>


    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">


    <style>
        body {
            font-family: Arial, sans-serif;
        }

        .card {
            border: 1px solid #ddd;
            border-radius: 8px;
            transition: transform 0.2s ease-in-out, box-shadow 0.2s ease-in-out;
            height: 450px;
        }

        .card:hover {
            transform: scale(1.05);
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
        }

        .card-img-top {
            border-bottom: 1px solid #ddd;
            height: 250px;
            object-fit: cover;
            object-position: center;
        }

        .card-title {
            font-weight: bold;
            color: #333;
            margin-top: 10px;
        }

        .card-text {
            font-size: 18px;
            color: #555;
            margin-bottom: 15px;
        }

        .container {
            padding-top: 20px;
            padding-bottom: 40px;
        }

        .welcome-text {
            font-style: italic;
        }
    </style>

</head>

<body>

    <%- include('menuCli') %>

        <div class="container mt-5">
            <% if (products && products.length> 0) { %>
                <h1 class="text-center mb-5 welcome-text">Welcome to Shopping</h1>
                <div class="row g-4">
                    <% products.forEach(product=> { %>
                        <div class="col-md-4">
                            <div class="card h-100 text-center shadow-sm">
                                <% if (product.image) { %>
                                    <img src="<%= product.image %>" alt="<%= product.name %>" class="card-img-top">
                                    <% } else { %>
                                        <div class="card-img-top">
                                            <p>Pas d'image disponible</p>
                                        </div>
                                        <% } %>
                                            <div class="card-body">
                                                <h5 class="card-title">
                                                    <%= product.name %>
                                                </h5>
                                                <p class="card-text"><strong>Prix:</strong> $<%=
                                                        product.price.toFixed(2) %>
                                                </p>
                                                <a href="/Products/detaille/<%= product._id %>"
                                                    class="btn btn-primary">Détail</a>
                                            </div>
                            </div>
                        </div>
                        <% }); %>
                </div>
                <% } else { %>
                    <div class="alert alert-info text-center">
                        <p>Aucun produit disponible.</p>
                    </div>
                    <% } %>
        </div>

        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

</body>

</html>